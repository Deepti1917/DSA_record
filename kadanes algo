/*We have solved this using the algorithm - KADANE'S ALGORITHM 
Initially , max = arr[0]  and current sum = 0 

In the loop from i = 0 to n 
1) current sum is there we do current sum +=arr[I]

2) max = max of (max , current sum )

3) if current sum is < 0 , reset it to 0 

Time complexity 0(n) 
Space O(1)*/


import java.util.* ;
import java.io.*; 

public class Solution {
	
	public static long maxSubarraySum(int[] arr, int n) {
         
		 long max_sum = arr[0] ;
		 long current_sum = 0 ; 

		for(int i = 0 ; i < n; i++){

			current_sum += arr[i];

			if(current_sum<0)
				current_sum = 0 ;
			
				max_sum = Math.max(max_sum,current_sum);	
		}
		return max_sum;
	}

}
